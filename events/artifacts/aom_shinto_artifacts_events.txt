namespace = aom_shinto_artifacts
############################
# Generate God Tier Artifacts
############################
aom_shinto_artifacts.001 = {
	scope = none
	hidden = yes
	immediate = {
		# Court Artifacts
        # Tamatebako
        while = {
			count = 1
			random_ruler = {
				limit = {
					NOT = { 
						has_character_flag = aom_seeded_relic 
						any_artifact = { has_variable = aom_tamatebako }
					}
					highest_held_title_tier >= tier_duchy
					culture = { has_cultural_pillar = heritage_japonic }
				}
				create_artifact_aom_tamatebako_effect = { OWNER = this }
				add_character_flag = {
					flag = aom_seeded_relic
					days = 1
				}
			}
		}

        # Raijin's Drums
        while = {
            count = 1
            random_ruler = {
                limit = {
                    NOT = { 
                        has_character_flag = aom_seeded_relic 
                        any_artifact = { has_variable = aom_raijins_drums }
                    }
                    highest_held_title_tier >= tier_duchy
                    culture = { has_cultural_pillar = heritage_japonic }
                }
                create_artifact_aom_raijins_drums_effect = { OWNER = this }
                add_character_flag = {
                    flag = aom_seeded_relic
                    days = 1
                }
            }
        }

        # Fūjin's Windbag
        while = {
            count = 1
            random_ruler = {
                limit = {
                    NOT = { 
                        has_character_flag = aom_seeded_relic 
                        any_artifact = { has_variable = aom_fujins_windbag }
                    }
                    highest_held_title_tier >= tier_duchy
                    culture = { has_cultural_pillar = heritage_japonic }
                }
                create_artifact_aom_fujins_windbag_effect = { OWNER = this }
                add_character_flag = {
                    flag = aom_seeded_relic
                    days = 1
                }
            }
        }

        # Yamata no Orochi’s Scale
        while = {
            count = 1
            random_ruler = {
                limit = {
                    NOT = { 
                        has_character_flag = aom_seeded_relic 
                        any_artifact = { has_variable = aom_yamata_no_orochi_scale }
                    }
                    highest_held_title_tier >= tier_duchy
                    culture = { has_cultural_pillar = heritage_japonic }
                }
                create_artifact_aom_yamata_no_orochi_scale_effect = { OWNER = this }
                add_character_flag = {
                    flag = aom_seeded_relic
                    days = 1
                }
            }
        }

		# Inventory Artifacts
		# Ame-no-Nuboko (Heavenly Jeweled Spear)
        while = {
			count = 1
			random_ruler = {
				limit = {
					NOT = { 
						has_character_flag = aom_seeded_relic 
						any_artifact = { has_variable = aom_ame_no_nuboko }
					}
					highest_held_title_tier >= tier_county
					culture = { has_cultural_pillar = heritage_japonic }
				}
				create_artifact_aom_ame_no_nuboko_effect = { OWNER = this }
				add_character_flag = {
					flag = aom_seeded_relic
					days = 1
				}
			}
		}
 
		# Uchide no kozuchi (Magic Hammer)
        while = {
            count = 1
            random_ruler = {
                limit = {
                    NOT = { 
                        has_character_flag = aom_seeded_relic 
                        any_artifact = { has_variable = aom_uchide_no_kozuchi }
                    }
                    highest_held_title_tier >= tier_county
                    culture = { has_cultural_pillar = heritage_japonic }
                }
                create_artifact_aom_uchide_no_kozuchi_effect = { OWNER = this }
                add_character_flag = {
                    flag = aom_seeded_relic
                    days = 1
                }
            }
        }

        # Ame-no-Ohabari (Heaven-Point-Blade-Extended)
        while = {
            count = 1
            random_ruler = {
                limit = {
                    NOT = { 
                        has_character_flag = aom_seeded_relic 
                        any_artifact = { has_variable = aom_ame_no_ohabari }
                    }
                    highest_held_title_tier >= tier_county
                    culture = { has_cultural_pillar = heritage_japonic }
                }
                create_artifact_aom_ame_no_ohabari_effect = { OWNER = this }
                add_character_flag = {
                    flag = aom_seeded_relic
                    days = 1
                }
            }
        }

        # Hirutama (Ebbing Jewel) - Tide Jewels
        while = {
            count = 1
            random_ruler = {
                limit = {
                    NOT = { 
                        has_character_flag = aom_seeded_relic 
                        any_artifact = { has_variable = aom_hirutama }
                    }
                    highest_held_title_tier >= tier_county
                    culture = { has_cultural_pillar = heritage_japonic }
                }
                create_artifact_aom_hirutama_effect = { OWNER = this }
                add_character_flag = {
                    flag = aom_seeded_relic
                    days = 1
                }
            }
        }

        # Mitsutama (Flowing Jewel) - Tide Jewels
        while = {
            count = 1
            random_ruler = {
                limit = {
                    NOT = { 
                        has_character_flag = aom_seeded_relic 
                        any_artifact = { has_variable = aom_mitsutama }
                    }
                    highest_held_title_tier >= tier_county
                    culture = { has_cultural_pillar = heritage_japonic }
                }
                create_artifact_aom_mitsutama_effect = { OWNER = this }
                add_character_flag = {
                    flag = aom_seeded_relic
                    days = 1
                }
            }
        }

        # Kinshikyū (Emperor Jimmu's Bow)
        while = {
            count = 1
            random_ruler = {
                limit = {
                    NOT = { 
                        has_character_flag = aom_seeded_relic 
                        any_artifact = { has_variable = aom_kinshikyu }
                    }
                    highest_held_title_tier >= tier_county
                    culture = { has_cultural_pillar = heritage_japonic }
                }
                create_artifact_aom_kinshikyu_effect = { OWNER = this }
                add_character_flag = {
                    flag = aom_seeded_relic
                    days = 1
                }
            }
        }
	}
}
		
############################
# Generate Legend Tier Artifacts
############################
aom_shinto_artifacts.002 = {
	scope = none
	hidden = yes
	immediate = {
		# Court Artifacts
		# Kusanagi no Tsurugi (Three Sacred Treasures)
        while = {
			count = 1
			random_ruler = {
				limit = {
					NOT = { 
						has_character_flag = aom_seeded_relic 
						any_artifact = { has_variable = aom_kusanagi_no_tsurugi }
					}
					highest_held_title_tier >= tier_duchy
					culture = { has_cultural_pillar = heritage_japonic }
				}
				create_artifact_aom_kusanagi_no_tsurugi_effect = { OWNER = this }
				add_character_flag = {
					flag = aom_seeded_relic
					days = 1
				}
			}
		}

		# Yata no Kagami (Three Sacred Treasures)
        while = {
            count = 1
            random_ruler = {
                limit = {
                    NOT = { 
                        has_character_flag = aom_seeded_relic 
                        any_artifact = { has_variable = aom_yata_no_kagami }
                    }
                    highest_held_title_tier >= tier_duchy
                    culture = { has_cultural_pillar = heritage_japonic }
                }
                create_artifact_aom_yata_no_kagami_effect = { OWNER = this }
                add_character_flag = {
                    flag = aom_seeded_relic
                    days = 1
                }
            }
        }

		# Yasakani no Magatama (Three Sacred Treasures)
        while = {
            count = 1
            random_ruler = {
                limit = {
                    NOT = { 
                        has_character_flag = aom_seeded_relic 
                        any_artifact = { has_variable = aom_yasakani_no_magatama }
                    }
                    highest_held_title_tier >= tier_duchy
                    culture = { has_cultural_pillar = heritage_japonic }
                }
                create_artifact_aom_yasakani_no_magatama_effect = { OWNER = this }
                add_character_flag = {
                    flag = aom_seeded_relic
                    days = 1
                }
            }
        }

        # Kojiki (Records of Ancient Matters)
        while = {
            count = 1
            random_ruler = {
                limit = {
                    NOT = { 
                        has_character_flag = aom_seeded_relic 
                        any_artifact = { has_variable = aom_kojiki }
                    }
                    highest_held_title_tier >= tier_duchy
                    culture = { has_cultural_pillar = heritage_japonic }
                }
                create_artifact_aom_kojiki_effect = { OWNER = this }
                add_character_flag = {
                    flag = aom_seeded_relic
                    days = 1
                }
            }
        }

        # Nihon Shoki (The Chronicles of Japan)
        while = {
            count = 1
            random_ruler = {
                limit = {
                    NOT = { 
                        has_character_flag = aom_seeded_relic 
                        any_artifact = { has_variable = aom_nihon_shoki }
                    }
                    highest_held_title_tier >= tier_duchy
                    culture = { has_cultural_pillar = heritage_japonic }
                }
                create_artifact_aom_nihon_shoki_effect = { OWNER = this }
                add_character_flag = {
                    flag = aom_seeded_relic
                    days = 1
                }
            }
        }

        # Seven-Branched Sword
        while = {
            count = 1
            random_ruler = {
                limit = {
                    NOT = { 
                        has_character_flag = aom_seeded_relic 
                        any_artifact = { has_variable = aom_seven_branched_sword }
                    }
                    highest_held_title_tier >= tier_duchy
                    culture = { has_cultural_pillar = heritage_japonic }
                }
                create_artifact_aom_seven_branched_sword_effect = { OWNER = this }
                add_character_flag = {
                    flag = aom_seeded_relic
                    days = 1
                }
            }
        }

		# Inventory Artifacts
		# Nihongo (3 Legendary Spears)
                while = {
            count = 1
            random_ruler = {
                limit = {
                    NOT = { 
                        has_character_flag = aom_seeded_relic 
                        any_artifact = { has_variable = aom_nihongo }
                    }
                    highest_held_title_tier >= tier_county
                    culture = { has_cultural_pillar = heritage_japonic }
                }
                create_artifact_aom_nihongo_effect = { OWNER = this }
                add_character_flag = {
                    flag = aom_seeded_relic
                    days = 1
                }
            }
        }

		# Otegine (3 Legendary Spears)
        while = {
            count = 1
            random_ruler = {
                limit = {
                    NOT = { 
                        has_character_flag = aom_seeded_relic 
                        any_artifact = { has_variable = aom_otegine }
                    }
                    highest_held_title_tier >= tier_county
                    culture = { has_cultural_pillar = heritage_japonic }
                }
                create_artifact_aom_otegine_effect = { OWNER = this }
                add_character_flag = {
                    flag = aom_seeded_relic
                    days = 1
                }
            }
        }

		# Tonbokiri (3 Legendary Spears)
        while = {
            count = 1
            random_ruler = {
                limit = {
                    NOT = { 
                        has_character_flag = aom_seeded_relic 
                        any_artifact = { has_variable = aom_tonbokiri }
                    }
                    highest_held_title_tier >= tier_county
                    culture = { has_cultural_pillar = heritage_japonic }
                }
                create_artifact_aom_tonbokiri_effect = { OWNER = this }
                add_character_flag = {
                    flag = aom_seeded_relic
                    days = 1
                }
            }
        }

        # Dōjigiri (Demon Child Slayer) - Tenka-Goken (Five Great Swords)
        while = {
            count = 1
            random_ruler = {
                limit = {
                    NOT = { 
                        has_character_flag = aom_seeded_relic 
                        any_artifact = { has_variable = aom_dojigiri }
                    }
                    highest_held_title_tier >= tier_county
                    culture = { has_cultural_pillar = heritage_japonic }
                }
                create_artifact_aom_dojigiri_effect = { OWNER = this }
                add_character_flag = {
                    flag = aom_seeded_relic
                    days = 1
                }
            }
        }

        # Onimaru (Demon Circle) - Tenka-Goken (Five Great Swords)
        while = {
            count = 1
            random_ruler = {
                limit = {
                    NOT = { 
                        has_character_flag = aom_seeded_relic 
                        any_artifact = { has_variable = aom_onimaru }
                    }
                    highest_held_title_tier >= tier_county
                    culture = { has_cultural_pillar = heritage_japonic }
                }
                create_artifact_aom_onimaru_effect = { OWNER = this }
                add_character_flag = {
                    flag = aom_seeded_relic
                    days = 1
                }
            }
        }

        # Mikazuki (Crescent Moon) - Tenka-Goken (Five Great Swords)
        while = {
            count = 1
            random_ruler = {
                limit = {
                    NOT = { 
                        has_character_flag = aom_seeded_relic 
                        any_artifact = { has_variable = aom_mikazuki }
                    }
                    highest_held_title_tier >= tier_county
                    culture = { has_cultural_pillar = heritage_japonic }
                }
                create_artifact_aom_mikazuki_effect = { OWNER = this }
                add_character_flag = {
                    flag = aom_seeded_relic
                    days = 1
                }
            }
        }

        # Ōtenta (Great Denta) - Tenka-Goken (Five Great Swords)
        while = {
            count = 1
            random_ruler = {
                limit = {
                    NOT = { 
                        has_character_flag = aom_seeded_relic 
                        any_artifact = { has_variable = aom_otenta }
                    }
                    highest_held_title_tier >= tier_county
                    culture = { has_cultural_pillar = heritage_japonic }
                }
                create_artifact_aom_otenta_effect = { OWNER = this }
                add_character_flag = {
                    flag = aom_seeded_relic
                    days = 1
                }
            }
        }

        # Juzumaru (Rosary) - Tenka-Goken (Five Great Swords)
        while = {
            count = 1
            random_ruler = {
                limit = {
                    NOT = { 
                        has_character_flag = aom_seeded_relic 
                        any_artifact = { has_variable = aom_juzumaru }
                    }
                    highest_held_title_tier >= tier_county
                    culture = { has_cultural_pillar = heritage_japonic }
                }
                create_artifact_aom_juzumaru_effect = { OWNER = this }
                add_character_flag = {
                    flag = aom_seeded_relic
                    days = 1
                }
            }
        }

        # Sugari No Ontachi
        while = {
            count = 1
            random_ruler = {
                limit = {
                    NOT = { 
                        has_character_flag = aom_seeded_relic 
                        any_artifact = { has_variable = aom_sugari_no_ontachi }
                    }
                    highest_held_title_tier >= tier_county
                    culture = { has_cultural_pillar = heritage_japonic }
                }
                create_artifact_aom_sugari_no_ontachi_effect = { OWNER = this }
                add_character_flag = {
                    flag = aom_seeded_relic
                    days = 1
                }
            }
        } 
	}
}